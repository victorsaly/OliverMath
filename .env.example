# Base URL for Azure Functions backend (do not include trailing slash)
# For local development:
VUE_APP_API_BASE_URL=http://localhost:7071
# For production (set in GitHub repo Settings > Secrets and variables > Actions > Variables):
# VUE_APP_API_BASE_URL=https://<your-functions-app>.azurewebsites.net

# Legacy env vars (deprecated - now handled by Azure Functions)
# VUE_APP_TOKEN_AI=<deprecated; use Azure Functions speechToken>
# VUE_APP_BASE_URL=<deprecated>
